
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@model List<ResultFeatureSliderDto>
@{
    int count = 0;
}
<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <h4>Öne Çıkan Görsel Listesi</h4>
                <p>Öne Çıkan Görseller ile ilgili işlemlerinizi aşağıdaki tablo üzerinden gerçekleştirebilirsiniz.</p>
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>Görsel Başlığı</th>
                            <th>Görsel Durumu</th>
                            <th>Sil</th>
                            <th>Güncelle</th>
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <th scope="row">@count</th>
                                <td>@item.Title</td>
                                <td>@item.Status</td>
                                <td><a href="/Admin/FeatureSlider/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
                                <td><a href="/Admin/FeatureSlider/Update/@item.Id" class="btn btn-info">Güncelle</a></td>
                            </tr>
                        }
                        </tbody>
                    </table>
                    <a href="/Admin/FeatureSlider/Create/" class="btn btn-success">Slider ekle</a>
                </div>
            </div>
        </div>

    </div>
</section>


